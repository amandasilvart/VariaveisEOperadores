//1. Escreva um algoritmo que defina um valor inteiro e escreva na tela seu valor.

integer valor = 5;
system.debug(valor);

//2. Escreva um algoritmo que tenha nome de produto e preço exiba na tela os valores.
String nomeDoProduto = 'Caneta';
decimal valorDoProduto = 1.50;
System.debug('O produto é ' + nomeDoProduto + '\n O valor é de: ' +
valorDoProduto);

//3. Escreva um algoritmo que defina um valor inteiro e escreva (na tela) o seu
antecessor.
integer n1=3;
integer n2= n1-1;
system.debug(n2);

//4. Escreva um algoritmo que tenha dois valores reais, some os valores e exiba o
resultado.
integer v1= 4;
integer v2= 2;
integer soma= v1+v2;

system.debug(soma);

//5. Escreva um algoritmo que receba uma idade, subtraia do ano atual e exiba o resultado, mostrando ‘Nascido em (valorDaSubtração)’.
integer idade=20;
integer anoAtual=2023;
integer anoNascimento= (anoAtual-idade);
system.debug('Nascido em '+anoNascimento);

//6. Escreva um algoritmo para definir as dimensões de um retângulo (base e altura), calcular e escrever a área do retângulo.
Decimal base=5;
Decimal altura=6;
Decimal area= base*altura;
System.debug('A área é: ' +area);

//7. Escreva um algoritmo para definir o salário mensal atual de um funcionário e o percentual de reajuste de 25%. Calcular e escrever o valor do novo salário com
o acréscimo do reajuste.
decimal salarioAtual=2.000;
Decimal reajuste= 0.25;
Decimal SalarioReajuste= salarioAtual*reajuste;
Decimal novoSalario= salarioAtual+SalarioReajuste;
system.debug('O valor final é de ' +novoSalario );

//8. Escreva um algoritmo que atribua 3 notas, calcule a média e exiba o resultado.
integer nota1=6;
integer nota2=8;
integer nota3=9;
decimal media= (nota1+nota2+nota3)/3;
system.debug('A média é '+ media);
//9. Faça um algoritmo que define três notas de um aluno, calcule e escreva a média final deste aluno. Considerar que a média é ponderada e que o peso das notas é 2, 3 e 5. Fórmula para o cálculo da média final é:

integer nota1=6;
integer nota2=8;
integer nota3=9;
decimal media= ((nota1*2)+(nota2*3)+(nota3*5))/10;
System.debug(media);
